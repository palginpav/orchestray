---
phase: 05-smart-model-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/orchestray:config/SKILL.md
  - agents/pm.md
autonomous: true
requirements: [ROUT-01, ROUT-03]

must_haves:
  truths:
    - "PM agent selects Sonnet for subtasks scoring 0-5 and Opus for subtasks scoring 6-12"
    - "PM agent assigns Haiku only to bounded utility tasks (formatting, boilerplate, simple file reads) and never to architect or reviewer roles"
    - "PM agent auto-escalates to a stronger model when a weaker model fails"
    - "Users can override routing via model_floor, force_model, haiku_max_score, opus_min_score config settings"
    - "Routing outcomes (model, score, pass/fail, escalation count) are logged in events.jsonl"
  artifacts:
    - path: "agents/pm.md"
      provides: "Section 19: Model Routing Protocol with routing rules, escalation, spawning integration"
      contains: "## 19. Model Routing Protocol"
    - path: "skills/orchestray:config/SKILL.md"
      provides: "Four new routing config settings"
      contains: "model_floor"
  key_links:
    - from: "agents/pm.md Section 19"
      to: "agents/pm.md Section 12"
      via: "complexity score feeds routing decision"
      pattern: "Section 12.*score"
    - from: "agents/pm.md Section 19"
      to: "agents/pm.md Section 3"
      via: "routing decision sets model parameter at spawn"
      pattern: "model:"
    - from: "agents/pm.md Section 19"
      to: ".orchestray/audit/events.jsonl"
      via: "routing_outcome event appended after agent completes"
      pattern: "routing_outcome"
---

<objective>
Add the model routing protocol to the PM agent so it selects Haiku/Sonnet/Opus per subtask based on complexity scoring, with auto-escalation on failure, user-configurable overrides, and routing outcome logging.

Purpose: Enables cost-quality optimization -- simple subtasks use cheaper models while complex tasks get the strongest model, saving users money without sacrificing quality.
Output: PM agent with Section 19 (Model Routing Protocol), updated Section 3 (model param at spawn), updated config skill with 4 new settings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-smart-model-routing/05-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Key structures the executor needs from the existing codebase -->

From agents/pm.md Section 12 (Complexity Scoring):
- Produces a numeric score 0-12 from 4 signals (file count, cross-cutting, description, keywords)
- Score mapping: 0-3=Simple, 4-7=Medium, 8-12=Complex
- Score is available after Section 0 evaluation

From agents/pm.md Section 3 (Agent Spawning):
- Spawns via Agent('{name}') tool call
- Dynamic agents get definition files in agents/ with YAML frontmatter including `model: inherit`
- Core agents: architect, developer, reviewer

From agents/pm.md Section 17 (Dynamic Agent Spawning):
- Creates temporary agent .md files with frontmatter: name, description, tools, model, maxTurns, color
- `model: inherit` is current default -- routing will change this to selected tier

From skills/orchestray:config/SKILL.md:
- Config stored in .orchestray/config.json
- Current 9 settings: auto_review, max_retries, default_delegation, verbose, complexity_threshold, force_orchestrate, force_solo, replan_budget, verify_fix_max_rounds
- Validation pattern: type checks + range checks + mutual exclusion

From agents/pm.md Section 15 (Cost Tracking):
- Events appended to .orchestray/audit/events.jsonl
- agent_stop events contain: timestamp, type, orchestration_id, agent_id, agent_type, usage, estimated_cost_usd
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add routing config settings to config skill</name>
  <files>skills/orchestray:config/SKILL.md</files>
  <read_first>skills/orchestray:config/SKILL.md</read_first>
  <action>
Extend the config skill with 4 new routing settings per D-05 and D-06.

Add these settings to the defaults JSON block (after `verify_fix_max_rounds`):

```json
"model_floor": "sonnet",
"force_model": null,
"haiku_max_score": 3,
"opus_min_score": 6
```

Add these rows to the "Available Settings" table:

| `model_floor` | string | `"sonnet"` | Minimum model tier for all agents: "haiku", "sonnet", or "opus" |
| `force_model` | string/null | `null` | Force all agents to use this model, overriding routing. One of: "haiku", "sonnet", "opus", or null |
| `haiku_max_score` | number | `3` | Maximum complexity score for Haiku routing (0-12). Tasks scoring above this get Sonnet or higher |
| `opus_min_score` | number | `6` | Minimum complexity score for Opus routing (0-12). Tasks scoring at or above this get Opus |

Add these validation rules to the Validation section:

- `model_floor` must be one of: "haiku", "sonnet", "opus"
- `force_model` must be one of: "haiku", "sonnet", "opus", or null
- `haiku_max_score` must be a number between 0 and 12
- `opus_min_score` must be a number between 0 and 12
- `opus_min_score` must be greater than `haiku_max_score` -- reject with error: "opus_min_score must be greater than haiku_max_score to avoid routing ambiguity."

Add the 4 new rows to the output format table (the "When showing settings" section) with matching descriptions.
  </action>
  <verify>
    <automated>grep -c "model_floor\|force_model\|haiku_max_score\|opus_min_score" skills/orchestray:config/SKILL.md | grep -q "^[89]\|^1[0-9]" && echo "PASS: All 4 routing settings present multiple times" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- `grep "model_floor" skills/orchestray:config/SKILL.md` returns at least 3 matches (defaults, table, validation, output)
- `grep "force_model" skills/orchestray:config/SKILL.md` returns at least 3 matches
- `grep "haiku_max_score" skills/orchestray:config/SKILL.md` returns at least 3 matches
- `grep "opus_min_score" skills/orchestray:config/SKILL.md` returns at least 3 matches
- `grep "opus_min_score.*must be greater than.*haiku_max_score" skills/orchestray:config/SKILL.md` returns 1 match
  </acceptance_criteria>
  <done>Config skill has 4 new routing settings with defaults, validation rules, and output formatting. model_floor defaults to "sonnet", force_model defaults to null, haiku_max_score defaults to 3, opus_min_score defaults to 6.</done>
</task>

<task type="auto">
  <name>Task 2: Add Section 19 Model Routing Protocol to PM agent and update Section 3 spawning</name>
  <files>agents/pm.md</files>
  <read_first>agents/pm.md, skills/orchestray:config/SKILL.md</read_first>
  <action>
Add a new Section 19 to agents/pm.md AFTER Section 18 (Verify-Fix Loop Protocol). This section implements the core routing logic per D-01, D-02, D-04.

**Section 19: Model Routing Protocol**

The section must contain these subsections:

### Routing Decision Table (per D-01)
After Section 12 produces a complexity score for each subtask, apply this routing:

1. Read `.orchestray/config.json` for routing overrides:
   - If `force_model` is set (not null): use that model for ALL agents. Skip all routing logic.
   - Otherwise, read `model_floor`, `haiku_max_score`, `opus_min_score`.

2. For each subtask in the task graph (Section 13 output), determine the model:
   - **Haiku**: ONLY for bounded utility tasks that score <= `haiku_max_score` (default 3) AND are one of: formatting/linting output, boilerplate/scaffold generation, simple file reads/lookups, grep/search operations. NEVER use Haiku for architect or reviewer roles.
   - **Opus**: For subtasks scoring >= `opus_min_score` (default 6) — architecture decisions, complex debugging, security audits, cross-cutting refactors, novel system design.
   - **Sonnet**: Everything else (default workload). Standard implementation, code generation, test writing, reviews of non-complex changes.

3. Apply `model_floor` enforcement: if the routed model is weaker than `model_floor`, upgrade to `model_floor`. Model strength order: haiku < sonnet < opus.

4. Check for natural language override in the user's original prompt: "use opus", "use haiku", "use sonnet" — if detected, override the routing decision for ALL subtasks.

### Transparency (per CONTEXT.md specifics)
When announcing orchestration (Section 0 Medium+ Task Path), include the model assignment:
```
Assigning to {role} ({model} -- score {N}/12)
```
Example: "Assigning to developer (Sonnet -- score 4/12)"

### Auto-Escalation Protocol (per D-02)
When an agent fails (status != success in Section 4 result parsing) or produces poor results (reviewer rejects in Section 18):

1. If current model is Haiku: retry with Sonnet. If Sonnet also fails: retry with Opus.
2. If current model is Sonnet: retry with Opus.
3. If current model is Opus: do NOT retry with a different model — escalate per Section 16 (re-planning) or Section 18 (verify-fix loop).
4. Track escalation count per subtask. Maximum 2 escalations per subtask (haiku->sonnet->opus).
5. Log each escalation in the routing outcome event.

### Routing Outcome Logging (per D-04)
After each agent completes (in Section 4 result processing), append a `routing_outcome` event to `.orchestray/audit/events.jsonl`:

```json
{
  "timestamp": "<ISO 8601>",
  "type": "routing_outcome",
  "orchestration_id": "<current orch id>",
  "task_id": "<subtask id>",
  "agent_type": "<architect|developer|reviewer|{dynamic}>",
  "model_assigned": "<haiku|sonnet|opus>",
  "complexity_score": <N>,
  "result": "<success|failure|escalated>",
  "escalation_count": <0|1|2>,
  "escalated_from": "<haiku|sonnet|null>"
}
```

### Integration Points
- **Section 12 output feeds Section 19**: After scoring, before Section 13 decomposition, determine model per subtask.
- **Section 3 spawning uses Section 19 output**: When spawning any agent (core or dynamic), set `model: {routed_model}` in the agent invocation. For core agents, pass the model parameter. For dynamic agents (Section 17), write `model: {routed_model}` in the frontmatter instead of `model: inherit`.
- **Section 4 result handling triggers routing outcome logging**: After parsing agent result, append the routing_outcome event.
- **Section 18 verify-fix loop triggers escalation**: On reviewer rejection, check if model escalation should happen before entering fix loop.

---

**Also update Section 3 (Agent Spawning):**

Add a subsection "### Model Assignment at Spawn" after the "### Good Patterns" subsection. Content:

```
### Model Assignment at Spawn

Every agent spawned during an orchestration MUST have its model set according to the
Section 19 Model Routing Protocol. Do NOT use `model: inherit` during orchestrations.

For core agents (architect, developer, reviewer): The Agent() tool call uses the model
determined by Section 19. Mention the model in the delegation message.

For dynamic agents (Section 17): Write `model: {routed_model}` in the frontmatter
of the generated agent definition file, replacing the default `model: inherit`.

Outside of orchestrations (simple task path), model selection does not apply.
```

**Also update Section 17 (Dynamic Agent Spawning):**

In the "Frontmatter template" code block, change `model: inherit` to `model: {routed_model_from_section_19}` and add a comment: `# Set by Section 19 Model Routing Protocol. Do NOT use 'inherit' during orchestrations.`
  </action>
  <verify>
    <automated>grep -c "## 19. Model Routing Protocol" agents/pm.md | grep -q "^1$" && grep -c "routing_outcome" agents/pm.md | grep -q "^[2-9]" && grep -c "model_assigned" agents/pm.md | grep -q "^[1-9]" && grep -c "escalation_count" agents/pm.md | grep -q "^[1-9]" && grep -c "Model Assignment at Spawn" agents/pm.md | grep -q "^1$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- `grep "## 19. Model Routing Protocol" agents/pm.md` returns exactly 1 match
- `grep "routing_outcome" agents/pm.md` returns 2+ matches (event type + logging instruction)
- `grep "model_assigned" agents/pm.md` returns 1+ match (in the event JSON)
- `grep "escalation_count" agents/pm.md` returns 1+ match (in the event JSON)
- `grep "escalated_from" agents/pm.md` returns 1+ match (in the event JSON)
- `grep "Model Assignment at Spawn" agents/pm.md` returns 1 match (new subsection in Section 3)
- `grep "haiku_max_score\|opus_min_score" agents/pm.md` returns 2+ matches (config references)
- `grep "model_floor" agents/pm.md` returns 1+ match (enforcement logic)
- `grep "force_model" agents/pm.md` returns 1+ match (override logic)
- `grep "routed_model" agents/pm.md` returns 1+ match (in Section 17 frontmatter template)
- Section 17 frontmatter template no longer contains `model: inherit` as default
  </acceptance_criteria>
  <done>PM agent has Section 19 with full routing protocol (decision table, transparency, auto-escalation, outcome logging), Section 3 has model assignment at spawn subsection, Section 17 uses routed model instead of inherit. Routing outcomes are logged to events.jsonl with model_assigned, complexity_score, result, escalation_count, escalated_from fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User config -> PM routing | User-supplied config values (model_floor, force_model, thresholds) influence model selection |
| PM prompt -> agent spawn | PM's routing decision determines which model processes user code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | .orchestray/config.json | accept | Config is user-owned; user can set any valid model. Validation in config skill prevents invalid values. Low risk -- user tampering with their own config is expected usage. |
| T-05-02 | Denial of Service | force_model override | mitigate | Validate force_model is one of haiku/sonnet/opus/null in config skill. Reject invalid values with error message. |
| T-05-03 | Information Disclosure | routing_outcome events | accept | Events contain model names and scores, not sensitive data. Events are in user-owned .orchestray/ directory. |
</threat_model>

<verification>
1. `grep "## 19. Model Routing Protocol" agents/pm.md` returns 1 match
2. `grep -c "model_floor\|force_model\|haiku_max_score\|opus_min_score" skills/orchestray:config/SKILL.md` returns 8+
3. `grep "routing_outcome" agents/pm.md` returns 2+ matches
4. `grep "Model Assignment at Spawn" agents/pm.md` returns 1 match
5. Config skill has validation for all 4 new settings
</verification>

<success_criteria>
- PM agent has a complete Section 19 that implements two-tier + Opus routing per D-01
- Auto-escalation protocol exists per D-02
- Four config settings are defined with defaults, validation, and display per D-05/D-06
- Routing outcome events are specified with all required fields per D-04
- Section 3 enforces model assignment at spawn time
- Section 17 dynamic agent template uses routed model instead of inherit
</success_criteria>

<output>
After completion, create `.planning/phases/05-smart-model-routing/05-01-SUMMARY.md`
</output>
